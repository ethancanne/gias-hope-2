import styles from './application.module.scss';

type Props = {};

const Application = (props: Props) => {
  return <div className={styles.container}>application</div>;
};

export default Application;
